[package]
name = "footos-platform-ios"
version = "0.1.0"
edition = "2021"
description = "iOS platform implementation for 足迹相册 (footos)"

[dependencies]
footos-core = { path = "../core" }
footos-shared = { path = "../shared" }
async-trait = "0.1"
tokio = { version = "1", features = ["fs", "sync"] }
