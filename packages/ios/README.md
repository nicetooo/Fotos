# Fotos iOS

Native iOS implementation using Swift & SwiftUI.

## Core Integration
Uses `fotos-core` (Rust) via FFI.

## Setup
1. Native Swift project structure.
2. Link `fotos-core` static library.
3. Bridge Rust functions using UniFFI or manual C bridges.
