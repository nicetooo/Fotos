# Fotos Android

Native Android implementation using Kotlin & Jetpack Compose.

## Core Integration
Uses `fotos-core` (Rust) via JNI.

## Setup
1. Native Android project structure.
2. Link `fotos-core` shared library (`.so`).
3. Bridge Rust functions using JNI or UniFFI.
